@page "/hot/{source?}"
@model SharpBlogX.Web.Pages.Apps.HotModel
@{ 
    ViewData["Title"] = "每日热点 - ";
}
<div class="hot-container">
    <div class="plus-container">
        <div class="hot-nav">
            @foreach (var item in Model.HotSources.Result)
            {
                <a class="@(item.Source == Model.Source ? "hot-active" : null)" href="/hot/@item.Source">@item.Source</a>
            }
        </div>
        <div class="hot-content plus-row plus-col-space20">
            <ul>
                @foreach (var item in Model.Hot.Result.Datas)
                {
                    <li><a target="_blank" href="@item.Url">@item.Title</a></li>
                }
            </ul>
        </div>
    </div>
</div>